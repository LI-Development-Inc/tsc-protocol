[package]
name = "tsc-cli"
version.workspace = true
edition.workspace = true
description = "Sovereign Command Line Interface for the Standalone Complex"

[dependencies]
tsc-proto = { path = "../tsc-proto" }

tokio = { workspace = true }
dirs  = { workspace = true }
